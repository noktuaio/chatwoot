# ONE-SHOT — Ondas A→E do Email Campaign Builder (manifesto autoritativo)

Data: 2026-06-12. Repo: `/root/docker-stacks/build/chatwoot-campaign-v4.14.1` (NÃO é git; backup em `/root/docker-stacks/backups/chatwoot-src-pre-oneshot-20260612.tar.gz`).
Prod atual: image `v4.14.1-20260612-d3`. Brand: Hub2you / empresa Autonomia. App: Chatwoot v4.14.1 EE fork, Vue 3 `<script setup>`, Tailwind tokens `n-*` only (sem CSS custom/scoped/inline no APP — e-mails compilados usam inline style, isso é regra de e-mail e é permitido DENTRO do MJML), i18n `en` + `pt_BR` (paridade obrigatória), components-next, ícones `i-lucide-*` (strings ESTÁTICAS no template — UnoCSS não vê classes dinâmicas).

## Regras gerais (TODOS os agentes)
- MVP happy-path, mínimo de código, sem defensive programming desnecessário. Remover código morto que substituir.
- vue-i18n v9: `{`, `}`, `@`, `|` são especiais em strings i18n. Exemplos Liquid `{{ nome }}` em i18n DEVEM ser `{'{{ nome }}'}`; `@` literal → `{'@'}`.
- Pundit: policy TOP-LEVEL com nome da classe do record (ex.: `EmailTemplatePolicy`), nunca namespaced.
- Ruby: rubocop 150 cols, `module/class` compacto. JS: eslint Airbnb+Vue3.
- NÃO tocar em arquivos de outro pacote (tabela de ownership abaixo). Necessidades cross-package → escrever em `/tmp/oneshot/contracts/<pacote>.md` (o integrador aplica).
- i18n: NÃO editar `en/campaign.json`/`pt_BR/campaign.json` (nem crm.json) diretamente — escrever fragmentos em `/tmp/oneshot/i18n/<pacote>.en.json` e `<pacote>.pt.json` (objeto JSON com o caminho completo aninhado a partir da raiz do arquivo alvo, com chave de topo `__target__: "campaign" | "crm"`). O integrador faz o merge.
- Rotas novas: NÃO editar `config/routes.rb` — declarar no contract file (bloco exato de código pronto pra colar + onde). O integrador aplica.
- Migrações: usar EXATAMENTE os timestamps reservados abaixo (evita colisão).
- Testes: NÃO escrever specs rspec. A validação é por smokes reais (fase TestBed) + visual.

## Fatos do código (verificados — NÃO redescobrir)
- `EmailCampaign` (tabela `email_campaigns`): account, sender_identity, name, subject, from_name, body_html, reply_to, status enum {draft 0, scheduled, sending, sent, paused, canceled, failed}, scheduled_at, sent_at, counters (recipients/sent/failed/suppressed + delivered/opened/clicked/bounced/complained/unsubscribed de Onda3), ses_configuration_set, last_error. Controller: `app/controllers/api/v1/accounts/email_campaigns/campaigns_controller.rb` (permite name, subject, from_name, body_html, reply_to, sender_identity_id). Jbuilder em `app/views/api/v1/accounts/email_campaigns/campaigns/`.
- `EmailCampaignRecipient`: campaign ref, name, email, status enum {pending 0, sent, failed, suppressed, delivered, opened, clicked, bounced, complained, unsubscribed?}, ses_message_id, attempts, last_event_at. `MAX_ATTEMPTS=3`, `register_attempt!`, `claim` atômico no DeliveryEngine (at-most-once — PRESERVAR).
- `EmailSuppression`: account, email, reason (hard_bounce/complaint/unsubscribe/manual), source. `suppressed_set_for(account)`.
- `EmailEvent`: recipient ref, event_type enum {delivered 0, open 1, click 2, bounce 3, complaint 4, unsubscribe 5}, url, occurred_at (indexado), payload jsonb. `EmailCampaigns::Tracking::EventRecorder` grava open/click (open dedupe por recipient).
- `EmailCampaigns::TemplateRenderer` (Liquid): drops atuais = contact.name/email + nome/email. Liquid já no bundle.
- `EmailCampaigns::RecipientImporter`: usa `CampaignImports::Parser` (csv/xlsx) + `CampaignImports::HeaderMapper.new(headers, mode: :email)` (mapeia name/email por aliases). HOJE descarta colunas extras.
- `EmailCampaigns::DeliveryEngine`: linha ~110 `from_email` = from_name + `sender_identity.from_email`; linhas 125-126 montam `List-Unsubscribe: <url>` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click` com URL PLACEHOLDER vinda de `delivery_config.rb`. Checa suppression antes de enviar. Throttle.
- Tracking próprio: `EmailCampaigns::Tracking::Token` usa `Rails.application.message_verifier(:email_campaign_tracking)` + wrap urlsafe-base64; rotas públicas `get 'email_campaigns/t/o/:token'`, `t/c/:token`, `post 'email_campaigns/sns'` em routes.rb (~linha 732). Controllers públicos em `app/controllers/email_campaigns/`.
- IA: `Crm::Ai::CredentialResolver.new(account:).resolve` → `{api_key:, api_base:, source:}` ou nil. `Crm::Ai::ResponsesClient.new(credential:).create(model:, instructions:, input:, schema:, reasoning_effort: 'low')` → texto de saída (schema => JSON estruturado). Ver model name usado nos serviços de followup (`grep -rn "model:" app/services/crm/` / enterprise) e usar o mesmo.
- `Account` tem `custom_attributes`, `internal_attributes`, `settings` (jsonb). Guardrail usa `internal_attributes['email_campaigns_paused']` (+ motivo/timestamp).
- FE: store vuex `app/javascript/dashboard/store/modules/emailCampaigns.js` + `emailSenderIdentities.js`; api `app/javascript/dashboard/api/emailCampaigns.js`, `emailSenderIdentities.js`, `emailCampaignReports.js`; mutation-types ~linha 254+. Páginas: `routes/dashboard/campaigns/pages/EmailCampaignsPage.vue` (lista + dialogs), `EmailSenderPage.vue`; compose dialog `components-next/Campaigns/Pages/CampaignPage/EmailCampaign/EmailCampaignDialog.vue`; details (upload/agendar) `EmailCampaignDetailsDialog.vue`; relatório `routes/dashboard/crm/pages/CrmCampaignManagementPage.vue` (i18n no `crm.json`, chaves CRM_CAMPAIGN_MANAGEMENT.*). Menu lateral: item "Domínios de e-mail" vem de `campaigns.routes.js` (linha ~82, name `campaigns_email_sender_index`) referenciado no `components-next/sidebar/Sidebar.vue` / config do menu de Campanhas — REMOVER só o item de menu, manter rota acessível por URL.
- chart.js `~4.4.4` JÁ no package.json (ver uso existente em `app/javascript/dashboard/components/widgets/chart/` ou similar — seguir padrão).
- Libs novas JÁ INSTALADAS: `grapesjs@0.23.2`, `grapesjs-mjml@1.0.8`, `mjml-browser@5.3.0`. Ler `node_modules/grapesjs-mjml/README.md` para a API exata (init, blocks, `mjml-get-code`). IMPORT SEMPRE LAZY (dynamic import na rota/página) — não engordar o bundle principal.
- SES sandbox: enviar só para `atendimento@hub2you.ai` / `rodrigo.victor@autonomia.site`. Domínio `hub2you.ai` VERIFICADO → qualquer from `@hub2you.ai` é aceito. Env real: `EMAIL_CAMPAIGN_AWS_*` (copiar do service prod p/ testbed).
- FRONTEND_URL prod = https://chat.autonomia.site (URLs públicas de tracking/unsubscribe derivam dela — ver como Tracking::Token monta open_url/click_url e seguir o MESMO padrão).

## Migrações reservadas (timestamps fixos; todas ADITIVAS)
1. `20260612120001_add_custom_data_to_email_campaign_recipients.rb` — `add_column :email_campaign_recipients, :custom_data, :jsonb, null: false, default: {}`.
2. `20260612120002_add_builder_fields_to_email_campaigns.rb` — `body_mjml :text`, `preheader :string`, `from_email :string` em email_campaigns.
3. `20260612120003_create_email_templates.rb` — account ref (FK), name (string null:false), body_mjml text, body_html text, timestamps; index [account_id, name].
4. (NÃO criar migração p/ unsubscribe — EmailSuppression.reason já cobre; recipient enum já tem estados; se faltar valor `unsubscribed` no enum do recipient, adicionar AO ENUM no model apenas se a coluna for integer com espaço — verificar; senão registrar evento + suppression sem novo status.)

## Pacotes & ownership (NÃO cruzar)

### B1 — Migrações + Models (backend)
Arquivos: as 3 migrações acima; `app/models/email_campaign.rb` (+`body_mjml/preheader/from_email`; validação: `from_email` blank OU formato e-mail terminando `@{sender_identity.domain}` — validar no save com erro claro `from_email_domain_mismatch`; `has_many_attached :builder_assets`); `app/models/email_campaign_recipient.rb` (nada além do necessário p/ custom_data); NOVO `app/models/email_template.rb` (belongs_to account, validates name presence/uniqueness por conta). NOVO `app/policies/email_template_policy.rb` (espelhar EmailCampaignPolicy, admin-only).
Contract: declarar p/ integrador permits novos no campaigns_controller (`:body_mjml, :preheader, :from_email`) e jbuilder exposições.

### B2 — Import/Placeholders/Render/Validação (backend)
Arquivos: `app/services/campaign_imports/header_mapper.rb` (mode :email: além de name/email, retornar TODAS as outras colunas com chave normalizada — normalização: trim, NFKD remove acentos, downcase, espaços/traços→`_`, remove chars não `[a-z0-9_]`; colisão pós-normalização → sufixo `_2`); `app/services/email_campaigns/recipient_importer.rb` (gravar `custom_data` com as colunas extras da linha; valores `to_s.strip`; manter dedupe/suppression/contadores atuais); `app/services/email_campaigns/template_renderer.rb` (merge `custom_data` nos drops — chaves reservadas nome/email/contact/unsubscribe_url vencem; expor `unsubscribe_url` REAL por recipient — usar o token/URL do pacote B3 via classe `EmailCampaigns::Unsubscribe::Token`); NOVO `app/services/email_campaigns/template_validator.rb` (extrai `{{ tokens }}` de subject+preheader+body_mjml||body_html via regex Liquid simples, compara com chaves disponíveis (defaults + DISTINCT jsonb_object_keys(custom_data) dos recipients), retorna `{available:[], used:[], missing:[], blank_counts:{chave=>n}}`); NOVO controller action no recipients/campaigns p/ `GET placeholders` e `GET validate` → IMPLEMENTAR em NOVO controller `app/controllers/api/v1/accounts/email_campaigns/template_tools_controller.rb` (actions `placeholders` e `validate`, fetch campaign por id, authorize EmailCampaign) p/ não tocar arquivos de outros. Rotas via contract.
Aceite: headers `Nome`, `EMAIL`, `Nome Corretor`, `VALOR_MENSAL`, `Ano-Modelo` → chaves `nome_corretor`, `valor_mensal`, `ano_modelo` e name/email mapeados.

### B3 — Descadastro real (backend, Onda D)
Arquivos: NOVO `app/services/email_campaigns/unsubscribe/token.rb` (message_verifier(:email_campaign_unsubscribe), wrap/unwrap urlsafe igual Tracking::Token; `url(recipient)` → `FRONTEND_URL/email_campaigns/u/<token>`); NOVO controller público `app/controllers/email_campaigns/unsubscribe_controller.rb` (GET `show` → landing HTML simples pt_BR standalone SEM layout do app (estilo inline mínimo ok — página pública), com botão POST confirmar; POST `create` → RFC 8058 one-click: decodifica token, cria `EmailSuppression` (reason: 'unsubscribe', source: 'link') idempotente, grava `EmailEvent` unsubscribe, atualiza campanha counters, responde 200 texto simples; token inválido → 200 genérico sem vazar nada); views ERB em `app/views/email_campaigns/unsubscribe/`; `app/services/email_campaigns/delivery_config.rb` + `delivery_engine.rb` SOMENTE nas linhas do List-Unsubscribe (apontar para a URL real por recipient; manter mailto fallback se houver). Rotas públicas via contract (`get 'email_campaigns/u/:token'` + `post 'email_campaigns/u/:token'`).
Aceite: POST com token válido suprime + evento + idempotente; GET landing 200; DeliveryEngine pula suprimidos (já existe — confirmar inalterado).

### B4 — Guardrails por tenant (backend, Onda D)
Arquivos: NOVO `app/services/email_campaigns/guardrail.rb` (janela 7 dias: bounce_rate = bounces/sent, complaint_rate = complaints/sent, mínimo 50 sends p/ avaliar; limites bounce>5% OU complaint>0.3% → `account.internal_attributes['email_campaigns_paused']={'at':..., 'reason':...}` + save; método `paused?(account)`; `resume!(account)` limpa); NOVO `app/jobs/email_campaigns/guardrail_sweep_job.rb` + cron (seguir padrão dos crons existentes em `config/schedule.yml` ou onde RetrySweepJob foi agendado — OLHAR e seguir); `delivery_engine.rb` APENAS adicionar early-return se `Guardrail.paused?(@campaign.account)` (marcar campanha paused + last_error claro). FE: NADA (banner é follow-up).
Aceite: conta acima do limite → engine recusa e pausa campanha; abaixo → envia.

### B5 — Builder/IA/Assets/Test-send/Templates (backend, Ondas B+C)
Arquivos NOVOS: `app/controllers/api/v1/accounts/email_campaigns/assets_controller.rb` (POST multipart `file` → `ActiveStorage::Blob.create_and_upload!` + attach em `campaign.builder_assets` → render `{url:}` permanente via `url_for`/rails_blob_url — seguir padrão de URL de anexo usado no app; limite 2MB, content_type image/*); `app/controllers/api/v1/accounts/email_campaigns/ai_controller.rb` (POST `generate` {brief} e POST `rewrite` {text, instruction}; resolve credencial via `Crm::Ai::CredentialResolver` (sem credencial → 422 `ai_not_configured`); chama `ResponsesClient.create` com schema JSON estrito `{subject, preheader, mjml, subject_variants[3]}` p/ generate e `{text}` p/ rewrite); NOVO `app/services/email_campaigns/ai/prompt_builder.rb` (system prompt: catálogo dos 10 blocos MJML com exemplos compactos, placeholders disponíveis (recebe lista), regras: SOMENTE tags mjml válidas, NUNCA <script>/javascript:, sempre terminar com bloco footer contendo `{{ unsubscribe_url }}`, português brasileiro); NOVO `app/services/email_campaigns/ai/sanitizer.rb` (valida/limpa MJML retornado: remove script/iframe/on*=/javascript:, garante presença de `{{ unsubscribe_url }}` — se faltar, appenda footer mínimo); `app/controllers/api/v1/accounts/email_campaigns/templates_controller.rb` (CRUD EmailTemplate index/create/destroy, admin via EmailTemplatePolicy); test-send: action `send_test` no NOVO controller `test_sends_controller.rb` (POST {to_email} → renderiza com PRIMEIRO recipient ou drops fake, injeta tracking NO-OP ou real, envia via Ses::Sender 1 e-mail; validar to_email formato).
Rotas via contract. Aceite: sanitizer mata script/javascript:; generate sem credencial → 422; com schema → JSON parseável.

### B6 — Relatórios nível RD (backend, Onda E)
Arquivos: `app/services/email_campaigns/reports/builder.rb` (ADICIONAR: `clicks_by_url(campaign)` group count url; `timeline(campaign, interval: 'hour'|'day')` séries delivered/open/click por bucket de occurred_at (date_trunc), desde sent_at até agora cap 30d; manter API atual intacta); `app/controllers/api/v1/accounts/email_campaigns/reports_controller.rb` (ADICIONAR actions `clicks`, `timeline`, `recipients` (paginado: id, name, email, status, attempts, last_event_at, opens/clicks count) e `export` (CSV text/csv com as colunas do drilldown)); jbuilders se necessário.
Rotas via contract (member do reports/campaign — seguir como o reports atual roteia). Aceite: com eventos semeados, clicks_by_url e timeline retornam buckets corretos; CSV válido.

### F1 — Builder core (FE, Onda B) — DONO do store emailCampaigns
Arquivos: NOVA página `app/javascript/dashboard/routes/dashboard/campaigns/pages/EmailBuilderPage.vue` (rota filha `email_campaigns/:campaignId/builder`, name `campaigns_email_builder`, LAZY via dynamic import no campaigns.routes.js — F1 é dono dessa edição de rota FE); NOVO composable/componente `components-next/Campaigns/Pages/CampaignPage/EmailCampaign/builder/GrapesEditor.vue` (monta GrapesJS+grapesjs-mjml via dynamic import dentro de onMounted; altura total; carrega `body_mjml` (ou template inicial mínimo com footer travado); expõe getMjml/getHtml via `mjml-get-code`); top bar: voltar, nome da campanha, botões Salvar (PATCH body_mjml+body_html via store update), "Enviar teste" (prompt e-mail → POST test_send), "Criar com IA" (abre dialog do F3 — stub se F3 atrasar), toggle preview desktop/mobile (GrapesJS devices). Store `emailCampaigns.js` + api `emailCampaigns.js` + mutation-types: F1 é o ÚNICO que edita esses 3 (adicionar: update já existe? conferir; `sendTest`, e o que F3/F4 declararem nos contracts — F1 implementa as actions que eles pedirem). Estilo: chrome do app com tokens n-*; o canvas GrapesJS usa o CSS próprio da lib (import do css da lib dentro do componente lazy — permitido pois é lib, não CSS custom nosso; se eslint reclamar de import css, usar import dinâmico).
Na EmailCampaignsPage: NÃO editar (F4 adiciona o botão "Abrir editor" lá — contract).
Aceite visual: canvas renderiza, blocos arrastáveis, salvar persiste body_mjml e body_html compilado.

### F2 — Biblioteca de blocos (FE, Onda B)
Arquivos NOVOS apenas: `.../EmailCampaign/builder/blocks.js` (registra no GrapesJS os 10 blocos MJML da Autonomia: hero c/ imagem+headline+CTA; oferta destacada; 3 cards de benefício; depoimento; grade de produtos 2x2; imagem+texto lateral; CTA grande; FAQ curto (3 itens); divisor/espaçador; rodapé legal TRAVADO (não removível: `removable:false, draggable:false, copyable:false` no componente) com `{'{{ unsubscribe_url }}'}`-equivalente — no MJML do bloco usar literal `{{ unsubscribe_url }}` (é conteúdo, não i18n) + endereço da empresa placeholder editável); `.../builder/starterTemplates.js` (3 templates MJML completos prontos: promocional, newsletter, transacional-like). Labels dos blocos via i18n fragment. Imagens placeholder: usar URLs data-URI leves ou placeholders do próprio grapesjs — sem assets externos.
Interface com F1: export default function(editor, opts) registrável — F1 chama `blocks(editor)`. Documentar no contract a assinatura.

### F3 — Copiloto IA (FE, Onda C)
Arquivos NOVOS apenas: `.../builder/AiComposerDialog.vue` (textarea briefing + seletor de template-base opcional + botão Gerar → chama API `aiGenerate` → recebe {subject, preheader, mjml, subject_variants} → emite `generated` p/ F1 aplicar no editor + campos; estados loading/erro humanizado incl. `ai_not_configured` → mensagem "Configure a chave de IA do CRM"); `.../builder/AiBlockActions.vue` (mini-menu "Melhorar com IA": reescrever/encurtar/mais persuasivo → `aiRewrite` no texto selecionado do bloco ativo — integração via prop editor); NOVO api file `app/javascript/dashboard/api/emailCampaignAi.js` (generate/rewrite — SEM vuex, axios direto padrão ApiClient). i18n fragments próprios. Contract: eventos/props esperados do F1.

### F4 — Placeholders + Compose/Details + menu (FE, Onda A)
Arquivos: `EmailCampaignDialog.vue` (campo novo "Remetente (e-mail)" from_email com hint `precisa ser @dominio-verificado`; campo preheader; CTA pós-criação "Salvar e abrir no editor" → router push builder); `EmailCampaignDetailsDialog.vue` (seção "Placeholders disponíveis": GET placeholders → chips clicáveis (copiam `{{ chave }}` p/ clipboard com useAlert); seção validação pré-envio: GET validate → lista missing/blank warnings; refresh após import); NOVO `.../builder/PlaceholderChips.vue` reutilizável (props placeholders, emite insert) — usado no details e exportado p/ F1 usar no builder (contract); `campaigns.routes.js`/config do menu: REMOVER item "Domínios de e-mail" do menu lateral (manter a rota); EmailCampaignsPage.vue: botão "Editor" por campanha draft (router-link builder). api: precisa de `placeholders(id)`/`validate(id)` → DECLARAR no contract p/ F1 adicionar no api/store (F4 NÃO edita store/api de campaigns).
i18n fragments. CUIDADO vue-i18n: chips mostram `{{ chave }}` — montar a string no JS (template literal), NUNCA em chave i18n com `{{` sem escape.

### F5 — Relatórios nível RD (FE, Onda E) — DONO de CrmCampaignManagementPage + emailCampaignReports.*
Arquivos: `CrmCampaignManagementPage.vue` (upgrade: gráfico de série temporal entregas/aberturas/cliques (chart.js padrão existente no app — copiar o wrapper de `dashboard/components/widgets/chart` se houver, senão componente chart local na pasta crm/components), tabela "Cliques por link" (url, cliques únicos/total), drilldown por destinatário paginado com busca, botão Exportar CSV (window.open no endpoint export), comparativo: tabela de campanhas com taxas lado a lado (dados do index atual), labels "aberturas ≈ aproximadas"); api `emailCampaignReports.js` (+clicks/timeline/recipients/export) e store se existir módulo. i18n em fragment `__target__:"crm"`.

### Integração (1 agente, após TODOS os impl)
- Aplicar contracts: routes.rb (blocos públicos + api), permits/jbuilder do campaigns_controller (B1), actions de store/api pedidas (conferir se F1 já fez), botão cross (F4→lista), assinatura blocks (F2→F1), PlaceholderChips export (F4→F1).
- Merge i18n fragments em en/pt campaign.json + crm.json; rodar script de paridade (abaixo); validar JSON.
- Gates mecânicos: `ruby -c` em todos .rb alterados; `node_modules/.bin/eslint --fix` nos .vue/.js alterados (depois eslint limpo SEM --fix); paridade i18n (script node flatten-diff em TODOS os arquivos tocados: campaign.json e crm.json). Corrigir o que quebrar (mecânico). NÃO rodar vite (fase seguinte).
Paridade: `node -e` flatten keys EN vs PT → vazio dos dois lados.

## Fase TestBed (1 agente) — testes REAIS pré-deploy
1. `vite build` produção (NODE_OPTIONS=--max-old-space-size=6144). Falhou → reportar e parar.
2. `docker build -f docker/custom/Dockerfile.crm -t chatwoot-campaign-import:oneshot-test .`
3. Subir testbed em Swarm (network_public NÃO é attachable por docker run — usar service):
   - `docker service create --name oneshot_pg --network network_public -e POSTGRES_PASSWORD=oneshot -e POSTGRES_DB=chatwoot_oneshot postgres:15-alpine`
   - service app `oneshot_app`: image oneshot-test, env COPIADO do service prod `chat-autonomia_chatwoot_app` (inspect → todas envs) SOBRESCREVENDO: `DATABASE_URL=postgres://postgres:oneshot@oneshot_pg:5432/chatwoot_oneshot`, `POSTGRES_*` coerentes, `REDIS_URL` igual prod (ok — smokes NÃO usam perform_later; chamar services com perform_now/direto), `FRONTEND_URL=http://172.17.0.1:3344`, `ENABLE_ACCOUNT_SIGNUP=false`, publish `--publish 3344:3000`, entrypoint padrão do container web (igual prod) — antes, rodar one-off service p/ `bundle exec rails db:create db:schema:load db:migrate` apontando pro pg (db:migrate DEVE aplicar TODAS as migrações email_* incl. as 3 novas — validar lista).
   - Seed via rails runner no testbed: account 'Oneshot', admin user confirmado (email oneshot@hub2you.ai senha forte conhecida), habilitar features que o gate exigir (ver `EmailCampaigns::Config.enabled?` — exige CRM_KANBAN_ENABLED/EMAIL_CAMPAIGN_ENABLED env no service + o que mais o Config checar), EmailSenderIdentity verified hub2you.ai from atendimento@hub2you.ai ses_configuration_set igual prod, campanha draft com body_mjml inicial, CSV import REAL via RecipientImporter com colunas do exemplo do PO (nome, email=atendimento@hub2you.ai, nome_corretor, valor_mensal, ano_modelo...).
4. Smokes (rails runner no testbed; cada um imprime PASS/FAIL marcador):
   a. importer custom_data + normalização (acentos/maiúsculas/espaços);
   b. renderer merge + unsubscribe_url real;
   c. validator missing/blank;
   d. unsubscribe: token → suppression idempotente + evento + counters;
   e. guardrail: semear taxas acima do limite → paused + engine recusa; resume → envia;
   f. from_email por campanha: validação domínio + uso no payload;
   g. **ENVIO REAL SES**: campanha 1 recipient atendimento@hub2you.ai, from suporte@hub2you.ai, subject com {{nome_corretor}} → DeliveryEngine.perform real → MessageId + status sent (env AWS do prod);
   h. reports: semear EmailEvents (clicks urls variados, occurred_at espalhado) → clicks_by_url/timeline/CSV corretos;
   i. sanitizer IA mata script/javascript:; ai generate sem credencial → erro limpo;
   j. templates CRUD;
   k. `Rails.application.eager_load!` OK no testbed.
5. Aguardar HTTP 200 em `http://172.17.0.1:3344`. Escrever `/tmp/oneshot/testbed.md` (URL, login/senha seed, ids criados, resultados PASS/FAIL).
6. NÃO derrubar os services (a fase visual usa; teardown no final).

## Fase Visual (1 agente, Playwright MCP)
Contra `http://172.17.0.1:3344` (login do testbed.md, viewport 1600x900): login → Campanhas → Campanhas de e-mail (lista ok, sem erro console) → criar campanha pelo dialog (com from_email) → abrir Builder: canvas GrapesJS renderiza, painel de blocos com os 10, arrastar 1 bloco, chips de placeholders visíveis, Salvar (PATCH 200), preview mobile toggle → "Enviar teste" UI presente → dialog IA abre e sem credencial mostra erro humanizado → Gestão campanhas (CRM): gráficos renderizam com dados semeados, tabela cliques por link, drilldown, botão export baixa CSV (status 200) → GET página pública unsubscribe do token semeado renderiza → menu lateral SEM "Domínios de e-mail" e aba Configuração da inbox INTACTA (zero regressão: abrir uma inbox de e-mail se houver no seed; senão pular) → screenshots em /tmp/oneshot/shots/. Console errors fatais = FAIL. Relatório /tmp/oneshot/visual.md.

## Reviews adversariais (7 paralelos, após visual)
R1 segurança pública (unsubscribe: enumeração/timing/idempotência/CSRF-exempt correto p/ RFC8058, tracking intacto, assets upload: authz/admin/content-type/limite, IDOR em template_tools/ai/test_send/reports export); R2 correção backend (importer normalização e colisões, validator regex Liquid falsos positivos, renderer precedência, guardrail janelas/divisão por zero); R3 motor/at-most-once (claim intacto, retry, suppression check, List-Unsubscribe URL por recipient correto, from_email fallback); R4 FE qualidade (vue-i18n traps {{ }}/@, ícones estáticos, lazy import efetivo — grep no bundle? conferir dynamic import, leaks de editor destroy no unmount); R5 zero-regressão core (campaigns.routes.js, Sidebar, ConfigurationPage intactos p/ outros canais, EmailCampaignsPage); R6 e-mail/deliverability (MJML do footer/blocos compila, unsubscribe presente sempre, sanitizer cobre casos, headers RFC8058); R7 migrações/dados (aditivas, defaults, índices, jsonb_object_keys performance com cap, CSV export streaming/limite).
Cada review retorna findings {severity: blocker|major|minor, file, issue, fix_hint}. Fix-loop: blockers+majors corrigidos por agentes-fix com ownership do pacote afetado; re-rodar gate pontual; minors documentados.

## Final
GO/NO-GO reviewer lê tudo (gates, smokes, visual, findings resolvidos) → relatório final em /tmp/oneshot/final_report.md. Teardown testbed (`docker service rm oneshot_app oneshot_pg`) SÓ após relatório (manter imagem oneshot-test). DEPLOY: NÃO FAZER — o humano aprova depois.
